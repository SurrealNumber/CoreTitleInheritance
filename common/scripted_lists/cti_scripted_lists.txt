core_title = { # scope is character
    base = held_title
    conditions = {
        is_title_core = yes
    }
}

core_title_count_to_limit = { # scope is character
    base = core_title
    conditions = {
        does_core_title_count_to_limit
    }
}

inconsistent_core_title = {
    base = core_title
    conditions = {
        NOT = {is_core_consistent}
    }
}