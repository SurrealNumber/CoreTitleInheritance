core_limit = { # scope is character
    else_if = {
        limit={ has_game_rule = half_domain_core_holding_limit }
        value = domain_limit
        divide = 2
        ceiling = yes
    }
    else_if = {
        limit={ has_game_rule = domain_core_holding_limit }
        value = domain_limit
    }
    else_if = {
        limit={ has_game_rule = ludicrous_core_holding_limit }
        value = 10000
    }
}

coring_holding_cost_flat = {value = major_prestige_loss}
coring_holding_refund_flat = {value = medium_prestige_gain}
coring_holding_refund_flat_full = {value = major_prestige_gain}

amount_over_core_limit = { # scope is character
    if = {
        limit = {NOT = {exists = var:core_count}}
        value = 0
    }
    else = {
        value = var:core_count
        subtract = core_limit
        min = 0 # sets lower bound of 0
    }
}

#over_core_limit_prestige_modifier = {
#    value = -0.05
#    multiply = over_core_limit
#    min = -0.5 # sets lower bound
#}

#Not used
#get_core_count = { # scope is a character
#    if = {
#    limit = {NOT = {exists = var:core_count}}
#        initialize_core_count
#    }
#    var:core_count
#}

core_count_from_list = {
    value = 0
    every_core_holding = { add = 1 }
}