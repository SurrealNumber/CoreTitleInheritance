action_over_core_title_limit = { # Scope is character? (or player?)
    type = alert
    icon = action_above_domain_limit
    is_dangerous = yes
    
    check_create_action = { # scope is player character
        if = {
            limit = {is_over_core_limit = yes}
            try_create_important_action = {
                important_action_type = action_over_core_title_limit
                #actor = root - Don't think this is necessary
            }
        }
    }
    
    effect = {
        execute_decision = manage_core_titles_decision
    }
}