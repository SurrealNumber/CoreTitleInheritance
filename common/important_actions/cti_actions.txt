action_over_core_holding_limit = { # Scope is character? (or player?)
    type = alert
    icon = action_above_domain_limit
    is_dangerous = yes
    
    check_create_action = { # scope is player character
        if = {
            limit = {is_over_core_limit = yes}
            try_create_important_action = {
                important_action_type = action_over_core_holding_limit
                #actor = root - Don't think this is necessary
            }
        }
    }
    
    effect = { # scope is character with additional targets defined in try_create_important_action above.
        open_view_data = { # want to expand core titles tab, but don't know how
            view = character
        }
        
        de_designate_all_core_titles_decision = {
			open_view_data = decision_detail
		}
        #execute_decision = manage_core_titles_decision
    }
}